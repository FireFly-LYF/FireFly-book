# FireFly 本地密钥（复制为 .env 后填写；勿提交 .env）
# 启动：.\start-all.ps1 会加载并校验下列变量非空后注入子进程。
# 禁止在 application.yml / gateway.yaml 写默认密钥。

JWT_SECRET=
ADMIN_JWT_SECRET=
ADMIN_PASSWORD=
INTERNAL_HMAC_SECRET=
MEDIA_SIGN_SECRET=
MYSQL_PASSWORD=
RABBITMQ_PASSWORD=

[project]
name = "firefly-ai-services"
version = "0.1.0"
description = "FireFly-book Python AI workspace (moderation, assistant)"
requires-python = ">=3.11"
readme = "README.md"

[tool.uv.workspace]
members = [
    "firefly-ai-common",
    "moderation-service",
    "assistant-service",
]

server:
  port: 9001

spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/user?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: 123456   # ??????
    driver-class-name: com.mysql.cj.jdbc.Driver
  rabbitmq:
    host: 127.0.0.1
    port: 5672
    username: guest
    password: guest

mybatis:
  configuration:
    map-underscore-to-camel-case: true   # user_name ? userName
  type-aliases-package: userservice.entity

# 与 Gateway jwt.secret 一致；Access 由网关验签后注入 X-User-Id
firefly:
  jwt:
    secret: phase3-dev-secret-change-me-32bytes!
    access-ttl: 30m
    refresh-ttl: 14d
    tenant: tenant-a
